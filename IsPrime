new loop, primecheck, stdoutAck('rho:io:stdoutAck') in {
    
    Conotract loop(@x) = {
     match x {
     [] Nil
     [Head...tail] => {
      new ret in {
    for (_<= ret) {
    
    loop!(tail)
    } | primecheck!(head, *ret)
    }
  }
}
